<!-- qhud:languages -->
<p align="center">
  <a href="../i18n/ko/docs/04-quality/RISK_REGISTER.md">한국어</a> · <strong>English</strong> · <a href="../i18n/zh-CN/docs/04-quality/RISK_REGISTER.md">简体中文</a>
</p>
<!-- /qhud:languages -->

# RISK_REGISTER — qhud

> Status: living · Date: 2026-09-07 · Owner: chquandogong
> Rewritten for v0.6.1. The register had stood since v0.4.0 and was entirely
> Linux-shaped, with no row for the failure that dominated the field. Status
> is now an explicit column instead of something encoded in a likelihood
> dash, and every realized risk carries the date it happened.

| ID  | Risk                                                                                                                                                                                                        | L       | I    | Status                                                                                                                                    | Control / trigger                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ---- | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | WebKitGTK transparency or DMABUF defects on some drivers: blank or opaque window                                                                                                                            | med     | med  | **realized** 2026-08-13                                                                                                                   | Both fragile paths are now disabled in code by default (`WEBKIT_DISABLE_DMABUF_RENDERER`, `WEBKIT_DISABLE_COMPOSITING_MODE`) with `QHUD_KEEP_*` opt-outs, not runbook advice. Neither was sufficient alone; the actual control is R13's guard. libEGL "DRI3 device" errors at launch are the tell. Linux only                                                                                           |
| R2  | Widget appears as a window in the GNOME overview and "hide all" gestures                                                                                                                                    | certain | low  | accepted                                                                                                                                  | Accepted quirk of the XWayland approach. Permanent fix is a companion GNOME Shell extension (ALTERNATIVES C, backlog). Linux only                                                                                                                                                                                                                                                                       |
| R3  | Fractional scaling enabled later blurs XWayland on GNOME 46                                                                                                                                                 | low     | med  | open                                                                                                                                      | A3. GNOME 47+ `xwayland-native-scaling` improves it. Document, do not engineer now. Linux only                                                                                                                                                                                                                                                                                                          |
| R4  | qmonster library API drift breaks the build on a revision bump                                                                                                                                              | med     | low  | open                                                                                                                                      | The `rev=` pin makes drift loud at compile time and `view.rs` is the single absorption point. Since v0.6.0 there are two dependency contexts — the canonical pin and the Windows-patched sibling checkout — so drift can now surface on one platform only; CI guards the canonical files with `git diff --exit-code`                                                                                    |
| R5  | Two writers on qmonster's directory corrupt TUI state                                                                                                                                                       | —       | high | **closed by design**                                                                                                                      | `NoopSink`, no writes under `~/.qmonster` (D-004). Note the precise claim: qhud persists its own state elsewhere (registry, fetch store, window geometry). Regression check: any qhud-attributable file under `~/.qmonster`                                                                                                                                                                             |
| R6  | No tray host leaves no quit affordance                                                                                                                                                                      | low     | low  | open                                                                                                                                      | The widget runs without a tray and logs that it did; the runbook documents `pkill -x qhud`. Windows behavior here is undocumented                                                                                                                                                                                                                                                                       |
| R7  | The 2 s poll cost creeps                                                                                                                                                                                    | low     | low  | open                                                                                                                                      | qmonster caches version probes per pane lifetime; the interval is one constant. The loop also checkpoints geometry every ~30 s and, on Linux, samples pixels every ~28 s                                                                                                                                                                                                                                |
| R8  | The Linux binary runs only where the webkit2gtk 4.1 runtime exists                                                                                                                                          | certain | low  | accepted                                                                                                                                  | Runtime dependencies are listed in the runbook; CI builds on the same Ubuntu baseline as the target                                                                                                                                                                                                                                                                                                     |
| R9  | Transparent window regions may still intercept desktop clicks                                                                                                                                               | low     | low  | open, unverified                                                                                                                          | Window bounds hug the rounded panel. Revisit if wallpaper clicks get eaten. From cross-validation                                                                                                                                                                                                                                                                                                       |
| R10 | Geometry restore lands on a disconnected monitor after a topology change                                                                                                                                    | med     | low  | open                                                                                                                                      | Tray → Reset position (CV-1); the runbook documents deleting the state file. The monitor-hotplug checklist row is still pending                                                                                                                                                                                                                                                                         |
| R11 | Desktop Icons NG re-enabled silently kills all real-mouse interaction                                                                                                                                       | med     | high | open                                                                                                                                      | D-010. Detection: no `ptr:` or `ui:` breadcrumb on a real click. The runbook has the disable one-liner; durable coexistence needs the companion extension                                                                                                                                                                                                                                               |
| R12 | Verification blind spot: synthetic X11 input passes while real input fails                                                                                                                                  | —       | high | **closed by protocol**                                                                                                                    | Interaction claims require compositor-path injection or a human hand, never synthetic input alone (D-010, TEST_PLAN)                                                                                                                                                                                                                                                                                    |
| R13 | **Frame presentation freezes at display sleep**: the compositor stops scheduling frames for the keep-below window and the toolkit's frame clock never resumes, so clicks and fetches keep working invisibly | high    | high | **realized, controlled** — 28 detections 2026-08-26 → 09-07                                                                               | D-017's guard hashes a footer strip every ~28 s and heals by hide-and-show, then re-exec. All 28 healed at the first rung, none operator-visible. Residual: the trigger is not reproducible on demand, so the ladder cannot be regression-tested end to end, and the re-exec rung has never fired in the field. Linux only — a Windows equivalent would go undetected because the guard is compiled out |
| R14 | **Provider wire-format drift** silently breaks a reading                                                                                                                                                    | high    | med  | **realized three times** — Codex lane meanings 2026-07-13; an explicit null limit map; `used_credits` becoming a float 2026-09-01         | D-019: integer-meaning numbers are read for their meaning, no single optional field may fail a body, and a rejection names the drifted field. `QHUD_EXTRA_DIAG=1` prints the identity-free sub-objects. Detection: a refresh error that names a field, in the widget's own stderr                                                                                                                       |
| R15 | The Windows binary requires the Edge WebView2 Runtime                                                                                                                                                       | certain | low  | accepted                                                                                                                                  | Stated as a prerequisite in the Windows guide and the release notes. The R8 analogue; no bundled runtime is shipped                                                                                                                                                                                                                                                                                     |
| R16 | The Windows build leaves a dirty tree or a torn lockfile                                                                                                                                                    | low     | med  | open, guarded                                                                                                                             | The build script restores the canonical lockfile bytes in a `finally` block and refuses to overwrite a modified sibling checkout; CI asserts the canonical files are unchanged. Documented constraint: never run two Cargo commands concurrently in one checkout                                                                                                                                        |
| R17 | **A stale reading renders as current**                                                                                                                                                                      | med     | high | **realized twice** — a 27-hour-old per-model chip beside live gauges (v0.4.0); a two-day-old snapshot while every refresh failed (v0.5.3) | Every non-live row carries its origin and age; snapshot-only per-model chips are dimmed and marked; expired windows are omitted entirely. The second case was correct behavior — the number was honestly dated — and the real fault was the silent refresh failure, now R14                                                                                                                             |
| R18 | **One account's numbers wear another's name**                                                                                                                                                               | med     | high | **realized** — an early merge stamped the first account onto every row                                                                    | Identity is (account, organization) (D-018); a refresh result is matched by config directory and account id; a saved snapshot is used only for the account signed in now (v0.6.0); a Codex body describing another workspace is dropped rather than mislabelled                                                                                                                                         |

## Not risks, by construction

Recorded here so they are not re-litigated. Running an OAuth refresh grant
(single-use rotated tokens would break the operator's login), installing Unix
signal handlers in this process (a reserved signal segfaults the webview),
logging a usage response body (it carries an account uuid and an email), and
taking keyboard focus (the widget is pointer-only) are all prohibited in code
and asserted by tests or protocol.
