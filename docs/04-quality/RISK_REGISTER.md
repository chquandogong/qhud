# RISK_REGISTER

> Status: living · Date: 2026-08-07 · Owner: chquandogong

| #   | Risk                                                                                         | Likelihood | Impact | Mitigation / trigger                                                                                                                            |
| --- | -------------------------------------------------------------------------------------------- | ---------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | WebKitGTK transparency/dmabuf defects on some drivers (blank or opaque window)               | med        | med    | RUNBOOK env fallbacks (`WEBKIT_DISABLE_DMABUF_RENDERER=1`); plan-B Electron recorded in ALTERNATIVES                                            |
| R2  | Widget appears as a window in GNOME overview / "hide all" gestures                           | certain    | low    | Accepted quirk of the XWayland approach; permanent fix = GNOME Shell extension (ALTERNATIVES C, roadmap)                                        |
| R3  | Fractional scaling enabled later → XWayland blur on GNOME 46                                 | low        | med    | ASSUMPTIONS A3; GNOME 47+ `xwayland-native-scaling` improves; document, don't engineer now                                                      |
| R4  | qmonster lib API drift breaks the build on rev bump                                          | med        | low    | `rev=` pin makes drift loud at compile time; view.rs is the single absorption point; roadmap = upstream versioned JSON export                   |
| R5  | Two writers on `~/.qmonster` corrupt TUI state                                               | —          | high   | **Designed out**: NoopSink, no persistence (D-004). Regression = any qhud write under `~/.qmonster`                                             |
| R6  | Tray icon absent (no AppIndicator host) leaves no quit affordance                            | low        | low    | Widget runs fine without tray; RUNBOOK documents `pkill qhud`; window close via tray only by design (no titlebar)                               |
| R7  | 2 s polling cost creeps (CLI `--version` probes, tmux calls)                                 | low        | low    | qmonster caches version probes per pane lifetime; poll interval constant in one place                                                           |
| R8  | Release binary runs only where webkit2gtk-4.1 runtime exists                                 | certain    | low    | RUNBOOK lists runtime deps; CI builds on ubuntu-24.04 to match target baseline                                                                  |
| R9  | Transparent window regions may still intercept desktop clicks (tauri#13070)                  | low        | low    | Window bounds hug the rounded panel (corners only); revisit if wallpaper clicks get eaten — from Codex cross-validation                         |
| R10 | Geometry restore lands on a disconnected monitor after topology change                       | med        | low    | Tray → Reset position (CV-1); RUNBOOK state-file reset; roadmap: clamp-to-visible-area on restore — from Codex cross-validation                 |
| R11 | DING re-enabled (operator or distro upgrade) silently kills all real-mouse interaction again | med        | high   | D-010: detection = `qhud ui:` breadcrumbs absent on click; RUNBOOK disable one-liner; durable coexistence = companion Shell extension (backlog) |
| R12 | Verification tooling blind spot: XTEST passes while real input fails                         | —          | high   | **Closed by protocol**: interaction claims require compositor-path injection (Mutter RemoteDesktop, TEST_PLAN) — never XTEST alone              |
